/*
 *
 * SagaContainer constants
 *
 */
const key = `SagaContainer`;
const path = `src/${key}/`

export const FLUSH_STATE = `${path}FLUSH_STATE`;

export const LOAD_LIST = `${path}LOAD_LIST`;
export const LOAD_LIST_SUCCESS = `${path}LOAD_LIST_SUCCESS`;