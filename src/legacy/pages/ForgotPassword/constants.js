const key = `ForgotPassword`;
const path = `src/${key}/`

export const FLUSH_STATE = `${path}FLUSH_STATE`;

export const FORGOT_PASSWORD = `${path}FORGOT_PASSWORD`;
export const FORGOT_PASSWORD_SUCCESS = `${path}FORGOT_PASSWORD_SUCCESS`;
export const FORGOT_PASSWORD_ERROR = `${path}FORGOT_PASSWORD_ERROR`;
