/*
 * CaseOverview Messages
 *
 * This contains all the text for the CaseOverview container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CaseOverview';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CaseOverview container!',
  },
});
