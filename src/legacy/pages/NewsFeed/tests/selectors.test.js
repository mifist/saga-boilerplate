// import { selectNewsFeedListDomain } from '../selectors';

describe('selectNewsFeedListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
