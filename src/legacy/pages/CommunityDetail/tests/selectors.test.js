// import { selectCommunityDetailDomain } from '../selectors';

describe('selectCommunityDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
