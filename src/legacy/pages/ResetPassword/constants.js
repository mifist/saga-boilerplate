const key = `ResetPassword`;
const path = `src/${key}/`

export const FLUSH_STATE = `${path}FLUSH_STATE`;

export const RESET_PASSWORD = `${path}RESET_PASSWORD`;
export const RESET_PASSWORD_SUCCESS = `${path}RESET_PASSWORD_SUCCESS`;
export const RESET_PASSWORD_ERROR = `${path}RESET_PASSWORD_ERROR`;