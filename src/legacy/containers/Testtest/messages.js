/*
 * Testtest Messages
 *
 * This contains all the text for the Testtest container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Testtest';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Testtest container!',
  },
});
