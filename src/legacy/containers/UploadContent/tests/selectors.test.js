// import { selectUploadContentDomain } from '../selectors';

describe('selectUploadContentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
