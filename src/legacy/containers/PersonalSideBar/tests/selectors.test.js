// import { selectPersonalSideBarDomain } from '../selectors';

describe('selectPersonalSideBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
