/*
 * CaseOverview Messages
 *
 * This contains all the text for the CaseOverview container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProfileSuggestions';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProfileSuggestions container!',
  },
});
