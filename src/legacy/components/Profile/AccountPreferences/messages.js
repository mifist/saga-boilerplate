/*
 * AccountPreferences Messages
 *
 * This contains all the text for the AccountPreferences component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AccountPreferences';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AccountPreferences component!',
  },
});
