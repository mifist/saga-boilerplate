/*
 * GalleryImage Messages
 *
 * This contains all the text for the GalleryImage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GalleryImage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GalleryImage component!',
  },
});
