/*
 * GalleryPDF Messages
 *
 * This contains all the text for the GalleryPDF component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GalleryPDF';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GalleryPDF component!',
  },
});
