/*
 * ChatNavigation Messages
 *
 * This contains all the text for the ChatNavigation component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ChatNavigation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChatNavigation component!',
  },
});
