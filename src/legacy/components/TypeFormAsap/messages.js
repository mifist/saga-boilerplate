/*
 * TypeFormAsap Messages
 *
 * This contains all the text for the TypeFormAsap component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TypeFormAsap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TypeFormAsap component!',
  },
});
