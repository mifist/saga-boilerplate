/*
 * VideoPlayer Messages
 *
 * This contains all the text for the VideoPlayer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.VideoPlayer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VideoPlayer component!',
  },
});
