/*
 * VideoPopupVimeo Messages
 *
 * This contains all the text for the VideoPopupVimeo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.VideoPopupVimeo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VideoPopupVimeo component!',
  },
});
